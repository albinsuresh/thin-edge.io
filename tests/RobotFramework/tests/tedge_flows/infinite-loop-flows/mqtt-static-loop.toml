# This flow defines an infinite MQTT loop:
# the output topic 'te/loop/test' matches input filter 'te/loop/+'.
name = "mqtt-fixed-output-loop"

[input]
mqtt.topics = ["te/loop/+"]

[output]
mqtt.topic = "te/loop/test"
